doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        title error
    body      
        block wrapper
            h1= message
            h2= error.status
            //pre #{error.stack}